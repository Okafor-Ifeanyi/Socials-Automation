# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Runtime state lives in data/ and is committed so the GitHub Action can carry
# it between runs — this is the git-as-database constraint to remove first when
# moving to multi-tenant. Archived legacy files are not worth versioning.
data/archive/
data/*.tmp
